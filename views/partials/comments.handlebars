<section>
  <div class="container d-flex">
  <div class="comments">
    {{#each this}}
    <section class="comment border border-success rounded-0 col-8">
      <div class="meta">
        {{user.username}} on {{ format_date created_at}}
      </div>
      <div class="text">
        {{comment_text}}
      </div>
    </section>
    {{/each}}
  </div>
  </div>
</section>